<div class="card">
  <div class="card-body">
    <h2 class="card-title mb-5">Sign up</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group">
        <%= f.label :name, class: 'form-label fw-bold' do %>
          Name <span class="text-danger">*</span>
        <% end %>
        <%= f.text_field :name, autofocus: true, class: 'form-control' %>
      </div>

      <div class="field form-group">
        <%= f.label :email, class: 'form-label fw-bold' do %>
          Email <span class="text-danger">*</span>
        <% end %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :user_type, class: 'form-label fw-bold' do %>
          User Type <span class="text-danger">*</span>
        <% end %>
        <%= f.select :user_type,  User::USERTYPE_ , {}, autofocus: true , class: 'form-control'%>
      </div>

      <div class="field form-group">
        <%= f.label :password, class: 'form-label fw-bold' do %>
          Password <span class="text-danger">*</span>
        <% end %>
        <% if @minimum_password_length %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %>
        <%= f.password_field :password, autocomplete: "new-password", class: 'form-control' %>
      </div>

      <div class="field form-group">
        <%= f.label :password_confirmation, class: 'form-label fw-bold' do %>
          Password Confirmation <span class="text-danger">*</span>
        <% end %>
        <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control' %>
      </div>

      <div class="actions form-group">
        <%= f.submit "Sign up", class: 'btn btn-primary mt-3' %>
      </div>
    <% end %>

  </div>
</div>